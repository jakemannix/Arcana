---
type: feature
files: [src/main.ts, src/glyphs.ts, src/catalog.ts, src/magic.ts, src/style.css, index.html]
last_updated: 2026-09-23
tags: [doctrack/type/feature, doctrack/status/active, doctrack/audience/claude]
---
# Browser editor

The current UI is two schools: Enchantment/group theory with sixteen folios, and Transmutation/category theory with two, plus one shared cantrip folio. Catalog generated by [[graduate-grimoire]]; not handwritten in UI. Default is first-isomorphism, or a known folio ID from the URL fragment.

Two CodeMirror 6 instances with StreamLanguage highlighting, reentrancy-guarded bidirectional synchronization, and transactional key clones. Navigation saves drafts in a per-tab Map. File imports reset to an unverified personal draft; restoring reloads the selected verified original. Folio drafts are saved before successful imports; an imported personal draft is recoverable through its navigation button. Failed imports leave the current draft intact. There is no localStorage or backend.

`isCheckedSource` requires exact match with the selected verified original, exact spell decoding, and all catalog verification gates. A changed theorem, even with perfect translation, is visibly unverified. The client never runs Lean. Round trips are debounced 160ms, but edits immediately invalidate the checked badge and failures keep it invalid. Exported drafts with a paused or mismatched translation can be reopened as unchecked drafts without losing either pane.

`proofFoldRange` hides implementation after `:=` or `⇰`, preserving every hypothesis and the full conclusion. Custom CodeMirror placeholders are keyboard-operable reveal buttons. Arcana proofs start folded; Veil/Reveal buttons bulk-fold/unfold. Unfold effects schedule a post-update measurement, then short-lived spark particles and an editor glimmer. Successful manual round trips also sparkle. Reduced-motion disables both; no continuous animation. Unit tests cover every shipped declaration and multiline signatures.

Dependencies: [[../components/translator]], [[graduate-grimoire]], [[../decisions/browser-translation]].

The masthead offers Magic / Side by side / Mathematics. `src/presentation.ts` changes visibility without reconstructing editors; mode switching preserves documents, history, folding, and synchronization. Magic mode hides all mathematical exposition, tutorials, shared comments, vocabulary, references, and the Lean pane. Mode-specific help and status wording preserve the original-versus-unchecked-draft distinction. The three buttons support arrow, Home, and End navigation.

A bounded visible-character effect samples graphemes from the old and new views, tumbles them into a jumble, then reforms them. Controls remain outside the animation; input, scrolling, repeated mode changes, resizing, and backgrounding cancel it. Reduced motion switches instantly. No persistent particles or infinite animation.

`src/comments.ts` uses the translator lexer to identify nested comments without mistaking strings or quoted identifiers for notes. CodeMirror replacement decorations hide comments without changing source, history, or translation. The shared Notes control toggles them in both editors; hidden notes can be opened individually. Magic mode suppresses note text and reveal buttons; leaving it restores the user's notes preference. Copy/export retains every comment. Proof folding masks comments and literals when finding boundaries but counts literal-only bodies as actual code; introductory comments stay outside the preceding proof fold.

`src/tutorial.ts` renders motivation, a collapsible four-step walkthrough, and an experiment with a separate hint, using text nodes. It is not a Lean checker. Tutorial data comes from the verified folio catalog, and is hidden in Magic mode.

## Glyph shortcuts
`src/glyphs.ts` holds two backslash-shortcut tables and a pure `expand` function, tested in `tests/glyphs.test.ts`. The Lean pane uses Lean's usual names (`\to`, `\-1`). The spell pane maps each Lean shortcut and each ASCII Lean symbol to its spell glyph (`\:` → ⟡), plus `\sp` ✨, `\dag` †, `\merc` ☿. A shortcut expands at once when no longer shortcut shares its prefix; otherwise space or Tab completes it. The handler runs at `Prec.highest` so bracket auto-closing cannot claim `\(` first. The help panel lists the common shortcuts.

The spell highlighter accepts both framed spell titles and ingredient identifiers with interior sparkles, such as `jade✨cube`. The help panel explains that these ingredients correspond to ordinary Lean variables, with unchanged types and hypotheses. Used ingredients and namespace aliases appear in the generated folio glossary. See [[../decisions/mathematical-names]].

Arcana branding is shared by the masthead, pane labels, metadata, and docs. The spell highlighter recognizes the translator's finite carrier-rune alphabet. Help explains the Veyr family and provides `\rune`, `\ankh`, `\moon`, and `\othala` shortcuts. Glossaries include all used carrier runes, including single-letter Greek variables. See [[../decisions/arcana-vocabulary]].

Spell numbers use positional kanji digits with `\0` through `\9` shortcuts, shared numeric highlighting, and an explanation in the reading guide. The math pane retains ordinary Lean literals; see [[../components/translator]].


`src/drafts.ts` shares production bundle serialization/parsing with regression tests. A valid-key `arcana/v1` bundle is a draft container, not a proof certificate: unfinished or mismatched panes are retained exactly and status checking reports their problem after loading. Malformed JSON/schema or ambiguous name keys fail before the current editors are replaced.

Tutorial/view release verification: original and decoded Lean builds plus axiom audit pass for 19 folios / 174 declarations; all 79 frontend tests pass. Browser checks covered note hide/reveal, Magic suppression, Math-only visibility, rapid mode changes, undo back to a checked original, malformed Arcana edits, per-folio draft retention, tutorial expansion, and the 180-glyph transition with no runtime errors. Browser file-picker automation was unavailable because Chrome's extension denied local file access; bundle save/reopen behavior is covered through the production parsing/serialization regression tests.
